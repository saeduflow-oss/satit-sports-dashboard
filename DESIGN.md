# เอกสารออกแบบ UX/UI — แดชบอร์ดกีฬาสาธิตสามัคคี 2569

เอกสารนี้อธิบายแนวคิดและข้อกำหนดการออกแบบของแดชบอร์ด เพื่อให้ทีมพัฒนา/ออกแบบ
ทำงานต่อได้โดยไม่ต้องเดา อ้างอิงเลย์เอาต์แบบ **Admin Dashboard** (แถบเมนูซ้าย +
พื้นที่เนื้อหาหลัก) ปรับให้เข้ากับบริบทการประกาศผลกีฬา

---

## 1. แนวคิดหลัก (Design Concept)

> "เปิดปุ๊บ รู้ทันทีว่าโรงเรียนเราอยู่อันดับไหน เหรียญเท่าไหร่ กำลังแข่งอะไรอยู่"

- **Glanceable first** — ข้อมูลสำคัญที่สุด (อันดับ/เหรียญ/รายการสด) อยู่บนสุด อ่านได้ใน 0.5 วินาที
- **เรียบหรู ทันสมัย** — พื้นผิวสะอาด เส้นขอบบาง เงานุ่ม ใช้กราฟช่วยเล่าเรื่องแทนตัวเลขล้วน
- **โทนสีคุมเข้ม** — น้ำเงิน (โครงสร้าง/ความน่าเชื่อถือ) + ชมพู (จุดสำคัญ/สด) + ขาว/เทา (พื้นหลัง)

---

## 2. โครงสร้างข้อมูล (Information Architecture)

```
แดชบอร์ด (หน้าหลัก)
├── แถบเมนูซ้าย (Sidebar)
│   ├── แดชบอร์ด            ← หน้าปัจจุบัน
│   ├── สรุปเหรียญ
│   ├── ชนิดกีฬา (15 ชนิด)
│   ├── ตารางแข่งขัน
│   ├── ผลการแข่งขันสด
│   ├── อันดับโรงเรียน
│   ├── ถ่ายทอดสด
│   └── ตั้งค่า
│
└── พื้นที่เนื้อหา (Main) — หน้าแรก: แถบเต็มความกว้างเรียงลงมา
    ├── แถบบน: สถานะ sync · แจ้งเตือนรายการสด · สลับธีม
    ├── แถบวันแข่งขัน (พื้นเข้ม): วัน/สถานะรายการ/กีฬาวันนี้  |  โรงเรียนเรา: อันดับ เหรียญ
    ├── ภาพประจำวัน (สไลด์เต็มความกว้าง) — แสดงเฉพาะวันที่มีภาพ
    ├── การ์ดแมตช์ (จำนวนคอลัมน์ผูกกับจำนวนการ์ด)
    └── อันดับเหรียญ 8 อันดับแรก  +  ประกาศผลล่าสุด (สูงเท่ากัน)
```

### กฎเลย์เอาต์: ห้ามเหลือช่องว่างจากข้อมูลที่มาไม่ครบ

ปริมาณข้อมูลแต่ละวันไม่เท่ากัน — บางวันไม่มีภาพ บางวันแข่งรายการเดียว เลย์เอาต์จึงต้อง
ยุบ/ขยายตามข้อมูล ไม่ใช่กริดตายตัวที่เหลือกล่องเปล่า

- **ส่วนที่ไม่มีข้อมูล ให้ซ่อนทั้งแถบ** ไม่ใช่แสดงกรอบว่าง (เช่น แถบภาพของวันที่ยังไม่มีภาพ)
- **แถบวันแข่งขันเป็นจุดยึดของหน้า** เพราะมีข้อมูลเสมอทุกวัน เป็นพื้นเข้มจุดเดียวของหน้า
- **จำนวนคอลัมน์ของการ์ดแมตช์ผูกกับจำนวนการ์ด** (คลาส `n1`–`n6`) ไม่ใช้ `auto-fit`
  ที่ปล่อยให้แถวสุดท้ายโหว่ ใบสุดท้ายของแถวที่ไม่เต็มจะกินคอลัมน์ที่เหลือ
  และเปลี่ยนเป็นการ์ดแนวนอนเมื่อกินทั้งแถว
- **พาเนลที่วางคู่กันสูงเท่ากันเสมอ** (`align-items: stretch`) วันที่ประกาศผลไปไม่กี่รายการ
  จะได้ไม่เหลือช่องโหว่ข้างตารางอันดับ

**ภาพประจำวัน** เป็นภาพของวันที่กำลังแข่งเท่านั้น (วันที่มีแมตช์สด → วันล่าสุดที่ประกาศผล →
วันแรก) ห้ามยืมภาพวันอื่นมาแสดง เพราะจะทำให้ผู้ใช้เข้าใจผิดว่าเป็นภาพวันนี้ —
ดูวิธีต่อข้อมูลใน [README.md](README.md)

**การ์ดแมตช์** ลำดับเนื้อหา: สถานะ+เวลา → สองฝั่งกับสกอร์ → ชื่อรายการ → กีฬา/สนาม
สีปรากฏเฉพาะป้ายสถานะกับตัวเลขสกอร์ที่กำลังสด ตัวการ์ดเป็นกลางเสมอ

**อันดับ 1–3 ในตารางเหรียญ** ใช้ป้ายอันดับสีทอง/เงิน/ทองแดง เพราะเป็นความหมายของอันดับนั้นจริง
ไม่ใช่สีตกแต่ง

### ตารางอันดับเหรียญ (`.board-panel` + `.mtable`)

โครงเดียวใช้ทั้งหน้าแรก (8 อันดับ) และหน้าอันดับเหรียญ (ทั้งหมด) — โค้ดอยู่ที่
`renderMedalTable()` ใน `public/js/common.js` แก้ที่เดียวเปลี่ยนทั้งสองหน้า

- **หัวพาเนลเป็นชื่อกับเส้นคั่น ไม่ใช่แถบสี** — แถบไล่เฉดม่วง→ชมพูของเดิมถูกถอดออกแล้ว
  เพราะขัดกับ anti-reference "การ์ดไล่เฉดสีจัดจ้าน" ใน PRODUCT.md และแย่งสายตาไปจากตัวเลขผล
  สีในหน้านี้สงวนไว้ให้สิ่งที่กำลังเกิดขึ้นจริง (ป้ายสด สกอร์สด) กับสีเหรียญที่มีความหมายเท่านั้น
- **ป้ายอันดับ** วงกลม 28px สีเหรียญทึบ (ไม่ไล่เฉด ไม่มีอิโมจิ) เฉพาะอันดับ 1–3 เท่านั้น
  อันดับอื่นเป็นตัวเลขเปล่า — วงกลมเทาทุกแถวคือสิ่งรกที่ไม่ได้บอกอะไรเพิ่ม
- **หัวคอลัมน์เป็นไอคอนเหรียญ + ข้อความ** (ทอง / เงิน / ทองแดง / รวม) — ไอคอน `.mt-mic` เป็น
  รูปทรง mask (`--mic-medal`: วงกลมเจาะดาว) ไม่ใช่อิโมจิ จึงย้อมสีตามชนิดเหรียญได้และหน้าตา
  เหมือนกันทุกแพลตฟอร์ม **ต้องมีข้อความกำกับคู่เสมอ** ตามข้อห้าม "ห้ามสื่อความหมายด้วยสีอย่างเดียว"
  (ไอคอนสามอันรูปทรงเดียวกัน ต่างกันแค่สี ลำพังไอคอนจึงไม่พอ)
- **ตัวเลขล้วน ชิดขวา ความกว้างตัวเลขคงที่** ไม่มีแถบสัดส่วนใต้ตัวเลข — ตัวเลขที่จัดชิดขวา
  เทียบกันได้ตรง ๆ อยู่แล้ว แถบสี 4 แท่งต่อแถวเป็นภาระสายตาที่ไม่ได้เพิ่มข้อมูล
- **ตัวเลขแต่ละคอลัมน์เป็นสีของเหรียญตัวเอง** (`--medal-*-ink`) สีเดียวกับไอคอนหัวคอลัมน์
  จับคู่คอลัมน์กับชนิดเหรียญได้โดยไม่ต้องไล่สายตากลับขึ้นไปอ่านหัวตาราง
  ยอดรวมเป็นสีแบรนด์และตัวใหญ่สุด เพราะเป็นตัวเลขที่คนมองหาเป็นอันดับแรก
- **ชื่อโรงเรียนใช้ชื่อเต็มตรงตามชีต** (ช่อง `fullName`) ไม่ย่อและไม่ตัดท้ายด้วย ellipsis
  เพราะเป็นข้อมูลทางการที่ต้องตรงกับต้นทาง และหลายโรงเรียนต่างกันที่ท้ายชื่อ
  (ฝ่ายมัธยม / วิทยาเขต…) ชื่อยาวจึงตัดขึ้นบรรทัดใหม่ด้วย `overflow-wrap: anywhere`
- **แถวของโรงเรียนเราไม่มีป้ายกำกับ** พื้น `--brand-100` บอกอยู่แล้ว และมีแถวเดียวในตาราง
  ป้ายซ้ำจึงเป็นสิ่งรก — โปรแกรมอ่านหน้าจอยังรู้จาก "(โรงเรียนของเรา)" ใน `aria-label`
- **จอแคบ (≤720px)** ซ่อนหัวตาราง ตัวเลขแต่ละช่องพกไอคอน+ป้ายกำกับของตัวเอง (ย่อไอคอนเหลือ
  11px และช่องไฟ 6px ไม่งั้นสามช่องเหรียญจะดันคอลัมน์รวมหลุดขอบบนจอ 360px) ชื่อโรงเรียน
  ตั้ง `overflow-wrap: anywhere` เพราะชื่อไทยเป็นคำยาวไม่มีช่องว่าง จะดันคอลัมน์รวมหลุดขอบ

### แถวตาราง `.stable` — ใช้ร่วมกันระหว่างตารางการแข่งขันกับตารางแข่งขัน

สองหน้านี้แสดงข้อมูลชุดเดียวกันคนละมุม (ผังกำหนดการ / ผลกับสถานะ) จึงใช้โครงแถวเดียวกัน
แก้ที่ `.st-*` ใน `styles.css` ที่เดียว เปลี่ยนทั้งสองหน้า

- **หนึ่งตารางอยู่ในหนึ่งพาเนล** หัวพาเนลบอกวันกับจำนวนรายการ ไม่ปล่อยแถวลอยบนพื้นหน้า
  เพราะแถวลอย ๆ อ่านเป็น "การ์ดหลายใบ" แทนที่จะเป็นตารางเดียว
- **คอลัมน์**: ผังกำหนดการ 3 ช่อง (เวลา / กีฬา–ประเภท / ระหว่าง) ·
  ตารางแข่งขันเพิ่ม `.stable.has-result` เป็น 5 ช่อง (+ ผล / สถานะ)
- **สกอร์เป็นตัวโตสุดของแถว** ชิดขวา ฟอนต์ตัวเลขความกว้างคงที่ · แมตช์ที่กำลังแข่งสกอร์เป็นสี `--live`
- **ป้าย "ประกาศแล้ว" ในตารางเป็นสีกลาง** ไม่ใช่เขียว เพราะขึ้นแทบทุกแถวจนกลายเป็นสีตกแต่ง
  (สีเขียว `--done` ยังใช้ตามเดิมในหน้าอื่นที่ป้ายนี้ขึ้นเป็นบางแถว)
- **ไอคอนกีฬาอยู่ในบรรทัดเดียวกับชื่อกีฬา** ไม่ใช่กล่องสี่เหลี่ยมแยกหน้าแถว ซึ่งเหลือเป็นกรอบว่าง ๆ
- **แถวที่โรงเรียนเราลงแข่ง** พื้น `--brand-100` เฉย ๆ ไม่ต้องมีป้ายกำกับซ้ำ เพราะชื่อเราอยู่ในช่อง
  "ระหว่าง" อยู่แล้ว และวันที่เราแข่งเกือบทุกรายการ ป้ายทุกแถวจะกลายเป็นสิ่งรก

---

## 3. เลย์เอาต์ & กริด (Layout & Grid)

- **Sidebar**: กว้างคงที่ 248px (ยุบเหลือไอคอนได้บนจอเล็ก), หัวแถบไล่เฉด น้ำเงิน→ชมพู
- **Main**: ยืดหยุ่น, ระยะขอบ 24–28px
- **กริดการ์ด**: ระบบ 8pt (ช่องไฟ 8/16/24px), การ์ดสรุปแถวละ 4 (auto-fit บนจอเล็ก)
- **แถวเนื้อหาล่าง** (`.board-row`): อันดับเหรียญ 1.7fr : ประกาศผลล่าสุด 1fr สูงเท่ากัน
  (เดิม 1.45fr — ขยายเพราะตารางใช้ชื่อโรงเรียนเต็มตามชีต ที่ 1.45fr ชื่อจะตกเป็น 3 บรรทัด
  ส่วนแผงประกาศผลมีที่เหลืออยู่แล้ว จึงยกความกว้างให้ตารางที่ต้องการมากกว่า)
  ยุบเป็นคอลัมน์เดียวที่ <1400px เพราะแคบกว่านั้นแผงตารางเหลือไม่ถึง 660px

---

## 4. คลังคอมโพเนนต์ (Component Inventory)

| คอมโพเนนต์ | หน้าที่ | สถานะที่ต้องมี |
|---|---|---|
| Sidebar item | นำทาง | default · hover · active |
| Hero slider | ภาพประจำวัน | มีภาพ (1/หลายภาพ) · ไม่มีภาพของวันนั้น |
| Match card | แมตช์สด/ถัดไป/ผลล่าสุด | สองฝั่งมีสกอร์ · ฝั่งเดียว (รวมทุกโรงเรียน) |
| Standings (8 อันดับ) | อันดับเหรียญย่อบนหน้าแรก | โรงเรียนเราอยู่ใน 8 อันดับ · อยู่นอก (ตรึงไว้ท้าย) |
| Board panel | พาเนลหัวเรียบ (ชื่อ + ป้ายจำนวน + ลิงก์ดูทั้งหมด) | มีตาราง (`.mtable`) · มีเนื้อหาอื่น (`.board-body`) |
| Medal table | ตารางอันดับเหรียญ ใช้ร่วมหน้าแรก/หน้าอันดับ | เต็มตาราง · ย่อ 8 อันดับ · จอแคบ (ยุบ 2 บรรทัด) |
| KPI panel + area chart | แนวโน้มเหรียญรายวัน/สัปดาห์ | มีข้อมูล · ไม่มีข้อมูล |
| Donut chart | สัดส่วน ทอง/เงิน/ทองแดง | render จากข้อมูลจริง |
| Gradient stat card | ตัวเลขเด่น 4 ใบ | default |
| Activity timeline | ผลการแข่งล่าสุด | มีผล · ยังไม่มีผล |
| Data table + badge | ตารางผลทุกรายการ | ค้นหา · กรอง · แบ่งหน้า |
| Status chip | สถานะ กำลังแข่ง/จบ/รอ | live · done · upcoming |

---

## 5. โทเคนสี (Color Tokens) — น้ำเงิน · ชมพู · ขาว · เทา

| โทเคน | ค่า (Light) | การใช้งาน |
|---|---|---|
| `--navy-900` | `#0E2A4E` | ข้อความหลัก / เมนู active |
| `--navy-700` | `#1E4E8C` | เหรียญทอง / กราฟเส้นอันดับ 1 |
| `--blue-500` | `#2F6FBF` | สถานะ "จบแล้ว" / accent น้ำเงิน |
| `--blue-100` | `#E5EFFB` | พื้นอ่อน |
| `--pink-500` | `#D6437C` | accent หลัก / LIVE / โรงเรียนเรา |
| `--pink-100` | `#FBE7F0` | ไฮไลต์แถว / พื้นชมพูอ่อน |
| `--gray-500` | `#8593A6` | เหรียญเงิน / ข้อความรอง |
| `--gray-200` | `#E3E8F0` | เส้นขอบ |
| `--white` | `#FFFFFF` | พื้นการ์ด |

**หลักการใช้สี 60-30-10**: ขาว/เทา 60% · น้ำเงิน 30% · ชมพู 10% (เฉพาะจุดสำคัญ)
สีเหรียญและสีสถานะทั้งหมดถูกจำกัดให้อยู่ในกรอบ น้ำเงิน/เทา/ชมพู เพื่อไม่ให้ชนกับ accent

**ไล่เฉดการ์ด (Gradient)** — ใช้เฉพาะน้ำเงิน↔ชมพู:
- การ์ด 1: `#D6437C → #A72C5C` (ชมพู)
- การ์ด 2: `#1E4E8C → #0E2A4E` (น้ำเงินเข้ม)
- การ์ด 3: `#2F6FBF → #1E4E8C` (น้ำเงิน)
- การ์ด 4: `#C43C74 → #1E4E8C` (ชมพู→น้ำเงิน)

---

## 6. ตัวอักษร (Typography Scale)

| ระดับ | ขนาด/น้ำหนัก | ฟอนต์ | การใช้ |
|---|---|---|---|
| Display | 30px / 600 | Noto Serif Thai | ตัวเลขเด่น (เหรียญรวม) |
| Title | 18px / 600 | Noto Serif Thai | หัวข้อพาเนล |
| Body | 14px / 400 | IBM Plex Sans Thai | เนื้อหาทั่วไป |
| Caption | 12px / 400 | IBM Plex Sans Thai | ป้ายกำกับ |
| Number | — / 500 | IBM Plex Mono | ตัวเลข/คะแนน (เรียงคอลัมน์ตรง) |

---

## 7. สถานะ Feedback (State Design)

ทุกส่วนที่มีข้อมูลเปลี่ยนแปลง ต้องออกแบบครบ:
- **Loading** — โครงร่าง skeleton ระหว่างรอ sync
- **Empty** — "ยังไม่มีผลการแข่งขันในวันนี้" แทนพื้นที่ว่าง
- **Error** — แจ้งเตือน + วิธีแก้ เมื่อเชื่อม Google Sheets ไม่สำเร็จ
- **Live** — จุดชมพูกะพริบ + ข้อความ "กำลังแข่ง"

---

## 8. Responsive

เนื้อหาหลักกว้างสูงสุด 1180px (`.main`) จอที่ใหญ่กว่านั้นจะเว้นขอบซ้ายขวาแทนการยืดตาราง
เพราะแถวที่กว้างเกินไปทำให้กวาดสายตาจากชื่อโรงเรียนไปหาตัวเลขไกลเกินจำเป็น

| Breakpoint | พฤติกรรม |
|---|---|
| ≥ 1400px | จอใหญ่: อันดับเหรียญ + ประกาศผล วางคู่กัน (1.7fr : 1fr) |
| ≥ 1200px | คอลัมน์ตัวเลขในตารางเหรียญกว้างขึ้น (ยกเว้นตารางย่อใน `.board-row`) |
| 861–1399px | แท็บเล็ตแนวนอน/โน้ตบุ๊ก: sidebar เต็ม + แผงล่างซ้อนกันเต็มความกว้าง |
| ≤ 860px | Sidebar ซ่อน (เปิดด้วยปุ่ม) — ครอบคลุมแท็บเล็ตแนวตั้ง 768/834 |
| ≤ 720px | ตารางเหรียญยุบเป็น 2 บรรทัด/แถว ตัวเลขพกไอคอน+ป้ายกำกับเอง |
| ≤ 560px | มือถือ: ระยะขอบ 14px · ช่องค้นหาเต็มบรรทัด · การ์ดชนิดกีฬา 2 คอลัมน์ |
| Touch target | ปุ่ม/เมนู ≥ 44×44px |

**บทเรียนจากบั๊กที่เคยเจอ** — media query วัดความกว้าง "จอ" ไม่ใช่ความกว้าง "แผง"
ตารางเหรียญที่อยู่ในแผงครึ่งเดียวของ `.board-row` จึงเคยโดนบีบจนคอลัมน์ชื่อเหลือ ~50px
และชื่อไทยเรียงลงแนวตั้งทีละตัวอักษร เวลาปรับ breakpoint ของตารางนี้ ต้องคิดถึงทั้ง
"อยู่เต็มความกว้าง" (หน้าอันดับเหรียญ) และ "อยู่ในแผงแคบ" (หน้าหลัก) เสมอ

---

## 9. Accessibility (WCAG AA)

- Contrast ข้อความ ≥ 4.5:1 ทุกจุด (รวมข้อความบนการ์ดไล่เฉด)
- ทุก icon-only button มี `aria-label`
- กราฟมี `<title>`/`role="img"` อธิบายด้วยข้อความ
- โฟกัสคีย์บอร์ดมองเห็นชัด (`:focus-visible`)
- รองรับ `prefers-reduced-motion` (ปิดอนิเมชันกะพริบ)

---

## 10. การเชื่อมต่อข้อมูล

หน้าเว็บดึง Google Sheets เองในเบราว์เซอร์ (`public/js/sheets.js`) → fallback `data/mock.json`
เว็บอยู่บน GitHub Pages จึงไม่มีโค้ดฝั่งเซิร์ฟเวอร์ · เปลี่ยนแหล่งข้อมูลได้โดยแก้ไฟล์นั้นให้คืน schema เดิม
รายละเอียดใน [README.md](README.md)
